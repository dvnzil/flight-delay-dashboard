export function Hero() {
  return (
    <section className="flex flex-col gap-3 py-16 sm:py-24">
      <h1 className="text-4xl sm:text-5xl font-semibold tracking-tight text-balance max-w-3xl">
        Where flights actually run late, and why.
      </h1>
      <p className="text-lg text-muted-foreground max-w-2xl">
        A look at 1.18M U.S. domestic flights from January and July 2024 — which
        airports and airlines run behind schedule, when it happens, and what&apos;s
        causing it.
      </p>
    </section>
  );
}
